/**
 * Déclarer une variable « colors » contenant un tableau vide.
 * Puis un second temps, ajouter "Rouge" et "Bleu" à ce tableau.
 * Enfin, afficher "Rouge" sur la page web, toujours en utilisant le tableau.
 */



const colors = [];
console.log(colors)

colors.push('Rouge')
colors.push('Bleu')

console.log(colors)
console.log(colors[0])

const ajout = ['Rouge', 'Bleu'];
console.log(ajout)
const numberOfColors = ajout.length;


for (let i=0; i<numberOfColors; ++i ) {
  console.log([i])
  vide.push([i])
  console.log(vide[i])
}


