const eleve = ['PF', 'Jeremy', 'Matthieu', 'Irene', 'Julien'];

const numbersOfeleve = eleve.length;
for (let i = 0; i < eleve; ++i) {
console.log(heroes[i]);
} 

const numbersOf = eleve.length;
for(i=0; i<numbersOf; ++i) {
  document.write(eleve[i]);
}